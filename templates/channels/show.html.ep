% layout 'default';

<h2><%= $message %></h2>

% # XXX fix this, abstract it away
<pre>
% foreach (@$logs) {
%   if ($_->type eq 'log') {
<%= $_->nice_ts %> - <%= $_->nick %>: <%= $_->entry %>
%   }
%   elsif ($_->type eq 'join') {
<%= $_->nice_ts %> - <%= $_->nick %> joined
%   }
%   elsif ($_->type eq 'part') {
<%= $_->nice_ts %> - <%= $_->nick %> left
%   }
% }
</pre>
